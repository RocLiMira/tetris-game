# Tetris Game

经典俄罗斯方块游戏，使用纯 HTML + JavaScript 实现，无任何框架依赖。

## 如何运行

直接在浏览器中打开 `index.html` 即可开始游戏，无需安装任何依赖。

```bash
# 或者使用任意静态文件服务器
npx serve .
```

## 技术栈

- HTML5 Canvas
- 原生 JavaScript (ES6+)
- CSS3

## 操作方式

- `←` `→` 左右移动
- `↑` 旋转方块
- `↓` 加速下落
- `Space` 直接落底
